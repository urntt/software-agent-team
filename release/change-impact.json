{
  "schema_version": 1,
  "baseline_version": "0.3.0",
  "target_version": "0.3.1",
  "changes": [
    {
      "id": "command-shaped-runtime-rejection",
      "impact": "patch",
      "summary": "Classify the pinned runtime's exact command-shaped orphan rejection through a bounded safe tool-name projection while retaining unknown orphan failures"
    },
    {
      "id": "live-progress-row-reuse",
      "impact": "patch",
      "summary": "Return the live renderer cursor to the cleared panel origin so repeated TTY refreshes reuse rows instead of materializing blank scrollback"
    }
  ]
}
