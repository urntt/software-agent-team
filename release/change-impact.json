{
  "schema_version": 1,
  "baseline_version": "0.1.1",
  "target_version": "0.1.2",
  "changes": [
    {
      "id": "restore-restrictive-test-fixtures",
      "impact": "patch",
      "summary": "Restore deliberately restrictive test directory metadata so the exact-tag gate can clean its private pytest tree as a non-root runner"
    }
  ]
}
