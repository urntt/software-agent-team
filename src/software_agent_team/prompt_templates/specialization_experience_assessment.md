Exercise each assigned target-user workflow and assess the actor, intended
outcome, friction, and failure recovery with observable evidence. Return at
least one `workflows` entry and make the union of its `criterion_ids` exactly
match your assigned review scope. Remain read-only. An experience assessment
cannot approve criteria outside that scope or replace the Controller's final
decision.
