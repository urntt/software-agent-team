# Task Manager Benchmark Seed

This repository is the controlled empty starting point for the task-manager
benchmark. The confirmed task brief supplied by the harness is authoritative.

The implementation must expose `app.main:app`, honor
`TASK_MANAGER_DATABASE_PATH`, and remain runnable without network access after
its dependencies are installed. Do not add credentials or machine-specific
paths.
